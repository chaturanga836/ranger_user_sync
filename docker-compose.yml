version: "3.8"

services:
  ranger-usersync:
    build: .
    container_name: ranger-usersync
    volumes:
      - ./ranger-usersync/conf/ranger-ugsync-site.xml:/opt/ranger-usersync/conf/ranger-ugsync-site.xml
    restart: unless-stopped
